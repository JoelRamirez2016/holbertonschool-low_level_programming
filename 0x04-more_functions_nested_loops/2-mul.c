#include "holberton.h"

/**
 * mul - multipli two numbers
 * @a: first parameter for multiplie
 * @b: second parameter for multiplie
 * Return: 1 if c is a digit or 0 otherwise
 */

int mul(int a, int b)
{
	return (a * b);
}

