#include "holberton.h"

/**
 * _isupper - check if c is a uppercase character
 * @c: parameter for check
 * Return: 1 if c is a digit or 0 otherwise
 */

int _isupper(int c)
{
	return (c >= 65 && c <= 90);
}
